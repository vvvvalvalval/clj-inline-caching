(ns vvvvalvalval.clj-inline-caching-test
  (:require [clojure.test :refer :all]
            [vvvvalvalval.clj-inline-caching :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
